// Example to dynamically populate the dropdown (optional)
document.addEventListener('DOMContentLoaded', () => {
    const recipeBlock = document.querySelector('.recipe-block');
  
    recipeBlock.addEventListener('mouseover', () => {
      // Fetch or display recipe details dynamically
      console.log('Hovered on the recipe block!');
    });
  });
  